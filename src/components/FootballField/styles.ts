import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    alignItems: 'center'
  },
  title: {
    textTransform: 'uppercase',
    color: '#FFF',
    fontWeight: 'bold',
    marginTop: 7,
    fontSize: 14
  }
});